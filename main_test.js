function nbYear(p0, percent, aug, p) {
    for (let i = p0; p; i = i + i * (percent / 100) + aug) {
        var counter = 0;
        counter += 1;
    }
    return counter;
}

var x = nbYear(1500, 5, 100, 5000);

